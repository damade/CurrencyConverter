package com.damilola.lib_compose_core.resources

import androidx.compose.ui.text.font.Font
import androidx.compose.ui.text.font.FontFamily
import androidx.compose.ui.text.font.FontWeight
import com.damilola.core_android.R

val arielFonts = FontFamily(
    Font(resId = R.font.ariel)
)

val asapFonts = FontFamily(
    Font(resId = R.font.asap),
    Font(resId = R.font.asap_bold, weight = FontWeight.Bold)
)

val gilroyFonts = FontFamily(
    Font(resId = R.font.gilroy_bold, weight = FontWeight.Bold)
)

val montserratFonts = FontFamily(
    Font(resId = R.font.montserrat_black),
    Font(resId = R.font.montserrat_bold, weight = FontWeight.Bold),
    Font(resId = R.font.montserrat_light, weight = FontWeight.Light),
    Font(resId = R.font.montserrat_medium, weight = FontWeight.Medium),
    Font(resId = R.font.montserrat_semibold, weight = FontWeight.SemiBold)
)

val poppinsFonts = FontFamily(
    Font(resId = R.font.poppins),
    Font(resId = R.font.poppins_bold, weight = FontWeight.Bold),
    Font(resId = R.font.poppins_light, weight = FontWeight.Light),
)

val ralewayFonts = FontFamily(
    Font(resId = R.font.raleway),
    Font(resId = R.font.raleway_bold, weight = FontWeight.Bold),
    Font(resId = R.font.raleway_thin, weight = FontWeight.Thin),
)



